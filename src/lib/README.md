# lib

Shared utilities and helper functions

This folder contains reusable logic that:

* is not tied to React components,
* can be used across the entire project,
* supports multiple patterns and features.

Typical examples:

* utility functions
* formatting helpers
* performance helpers
* shared non-visual logic

## 📌 Rule:
Nothing in this folder should import React components.