# Store

Global state management

This folder contains all logic related to:

* shared/global state,
* stores and contexts,
* application-level state such as selected patterns.

Typical examples:

* selected pattern state
* global contexts
* reducers or store definitions (if applicable)

## 📌 Rule:
The store should manage state and actions only, never UI rendering.